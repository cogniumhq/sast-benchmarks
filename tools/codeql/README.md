# CodeQL Tool Lane

Configuration, commands, and notes for CodeQL benchmark runs.

Each run should record:

- CodeQL CLI version
- query suite
- database creation command
- analysis command
- raw SARIF path
